<%- include('../partials/header') %>
<table id="list">
    <tbody>
        <% sneakers.forEach(function(s) { %>
            <tr>
                <td><%= s.name %></td>
                <td><%= s.price %></td>
            </tr>
            <% }); %>
    </tbody> 
</table>
<%- include('../partials/footer') %>